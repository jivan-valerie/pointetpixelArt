{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <div class="container col-md-10 col-lg-6 my-3">

    <h4 class="text-center">Détails</h4>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ category.info }}</td>
            </tr>
        </tbody>
    </table>
        <div class="d-flex justify-content-between mt-4">

            <a href="{{ path('category_index') }}"><button class="firstitem fs-5">Retour</button></a>

            <a href="{{ path('category_edit', {'id': category.id}) }}"><button class="secondbuttonitem fs-5">Modifier</button></a>

            {{ include('category/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
