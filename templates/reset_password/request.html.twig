{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <div class="container col-md-8 col-lg-6 mt-5">
    <h3 class="text-center">Reset votre mot-de-passe</h3>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        <div class="mt-3">
            <small>
               Veuillez entrer votre adresse mail. Vous recevrez un lien pour réinitialiser votre mot-de-passe.  
                {# Enter your email address and we we will send you a
                link to reset your password. #}
            </small>
        </div>
    <div class="text-end">
        <button class="btn favorite styled my-3">Envoyer</button>
    </div>
    {{ form_end(requestForm) }}
    </div>
{% endblock %}
