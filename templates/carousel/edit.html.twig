{% extends 'base.html.twig' %}

{% block title %}Edit Carousel{% endblock %}

{% block body %}
    <h3>Modifier Slide</h3>
{# edit un tableau selon son id, son form de formulaire pour pouvoir le mmodifier et de le supprimer #}
    <div class="d-flex justify-content-center">

    {{ include('carousel/_form.html.twig', {'button_label': 'Update'}) }}
</div>
    <a href="{{ path('carousel_index') }}"><button class="firstitem fs-5">Retour</button> </a>

    {{ include('carousel/_delete_form.html.twig') }}
{% endblock %}
