{% extends 'base.html.twig' %}

{% block title %}Artnumerique{% endblock %}

{% block body %}
    <h2 class="text-center mt-5">Détails vidéos</h2>
    
    <div class="container col-md-10 col-lg-6 my-3">
    
        <h4 class="text-center">Détails de la vidéo : {{ artnumerique.titre }}</h4>
    
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ artnumerique.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ artnumerique.titre }}</td>
                </tr>
                <tr>
                    <th>Auteur</th>
                    <td>{{ artnumerique.auteur }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ artnumerique.description }}</td>
                </tr>
                <tr>
                    <th>Iframe</th>
                    <td>{{ artnumerique.iframe }}</td>
                </tr>
            </tbody>
        </table>
        <div class="text-end" >
        
            <a href="{{ path('dash_video') }}"><button class="firstitem fs-5">Retour</button></a>

        </div>


    {{ include('oeuvre/_delete_form.html.twig') }}
    </div>
{% endblock %}
