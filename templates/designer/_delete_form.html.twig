<form method="post" action="{{ path('artnumerique_delete', {'id': artnumerique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artnumerique.id) }}">
    <button class="btn alert alert-danger p-1">Supprimer</button>
</form>
