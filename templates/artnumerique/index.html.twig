{% extends 'base.html.twig' %}

{% block title %}Artnumerique index{% endblock %}

{% block body %}
    <h1>Artnumerique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Auteur</th>
                <th>Description</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for artnumerique in artnumeriques %}
            <tr>
                <td>{{ artnumerique.id }}</td>
                <td>{{ artnumerique.titre }}</td>
                <td>{{ artnumerique.auteur }}</td>
                <td>{{ artnumerique.description }}</td>
                <td>{{ artnumerique.image }}</td>
                <td>
                    <a href="{{ path('artnumerique_show', {'id': artnumerique.id}) }}">show</a>
                    <a href="{{ path('artnumerique_edit', {'id': artnumerique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('artnumerique_new') }}">Create new</a>
{% endblock %}
