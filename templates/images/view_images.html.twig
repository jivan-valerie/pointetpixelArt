{% extends 'base.html.twig' %}


{% block title %}
	Images
{% endblock %}

{% block body %}

<main>
					<div class=" container">
                		<div class="row">
						{% for item in liste_images %}
						<div class="col-sm-10 col-md-6 col-lg-4 col-xl-3 mt-5">
							<div class="card h-100">
								<img src="{{asset('images/' ~ item.image)}}" class="card-img-top" alt="Aperçu du tableau cheveaux sauvages">
								<div class="card-body">
									<h5 class="card-title">
										{{ item.auteur }}</h5>
									<p class="titre">
										{{ item.titre }}</p>
								</div>
							</div>
						</div>
						{% endfor %}
						</div>
					</div>
						

</main>
				{% endblock %}